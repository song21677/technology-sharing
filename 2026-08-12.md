컴포넌트 스캔
1. 자동 주입
- @Configuration등 붙은 모든 클래스 빈으로 등록
- Spring Boot는 @SpringBootApplication이 같은 역할을 해준다.
- basePackages를 지정하지 않으면 @ComponentScan이 붙은 클래스의 패키지가 시작 위치가 된다.

2. 수동 주입: @Bean

자동 vs 수동 중복의 경우 수동 우선 적용된다.
최신 spring boot는 에러뱉음.


생성자 주입
- 불변, 필수
- 생성자가 하나일 때 @Autowired를 생략해도 자동 주입된다.
- 주입할 대상이 없어도 동작하게 하려면 @Autowired(required = false)

수정자 주입
- 변경